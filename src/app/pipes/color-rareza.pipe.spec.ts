import { ColorRarezaPipe } from './color-rareza.pipe';

describe('ColorRarezaPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorRarezaPipe();
    expect(pipe).toBeTruthy();
  });
});
